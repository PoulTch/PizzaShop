<h2>Welcome to PizzaShop!</h2>

<div class="row">
	<% @products.each do |p| %>
 	<div class="col-md-3">  
		<div><%= p.title %></div>
		<img src="<%= p.path_to_image %>" />
		<div><%= p.description %></div>
		<div>Size: <%= p.size %> сm</div>
		<div>Price: <%= p.price %> rub</div>
		<div><button type="button" class="btn btn-success btn-sm" onclick="add_to_cart(<%= p.id %>)">Add to cart</button></div>		 
	</div>
	<% end %>

</div>
<br>
<br>
<button type="button" class="btn btn-primary" onclick="something()">Click me</button>